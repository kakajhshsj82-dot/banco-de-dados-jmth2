<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BANCO DE DADOS JMTH</title>
    <style>
        body { font-family: 'Courier New', Courier, monospace; background-color: #0d1117; color: #c9d1d9; padding: 20px; }
        .container { max-width: 800px; margin: auto; }
        h1 { color: #58a6ff; border-bottom: 2px solid #58a6ff; }
        .card { background: #161b22; border: 1px solid #30363d; padding: 15px; margin-bottom: 20px; border-radius: 8px; }
        .card h2 { color: #79c0ff; margin-top: 0; }
        code { background: #000; color: #ff7b72; padding: 2px 5px; border-radius: 4px; }
        .prompt-box { background: #21262d; border-left: 5px solid #238636; padding: 10px; font-size: 0.9em; }
        .copy-btn { background: #238636; color: white; border: none; padding: 5px 10px; cursor: pointer; border-radius: 5px; margin-top: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🧠 Banco de Dados - IA JMTH</h1>
        <p>Copie as regras abaixo e cole no <code>SYSTEM_PROMPT</code> da sua IA.</p>

        <div class="card">
            <h2>1. Matemática (Regra de Ouro)</h2>
            <p>Para evitar erros de precedência como 2+2x2.</p>
            <div class="prompt-box">
                Sempre use PEMDAS: 1º Parênteses, 2º Multiplicação/Divisão, 3º Adição/Subtração. 
                Nunca calcule na ordem de leitura se houver sinais mistos.
            </div>
        </div>

        <div class="card">
            <h2>2. Scripts Roblox (Lua)</h2>
            <p>Regras para executores Delta e Ducky.</p>
            <div class="prompt-box">
                - Use <code>task.wait()</code> em loops.<br>
                - Use <code>game:GetService()</code> para serviços.<br>
                - Priorize performance: Scripts leves para Mobile.
            </div>
        </div>

        <div class="card">
            <h2>3. Segurança e Leis</h2>
            <p>Bloqueio de pedidos ilegais.</p>
            <div class="prompt-box">
                Se pedirem invasão ou roubo: "Invasão de sistemas é crime (Art. 154-A do Código Penal)."
            </div>
        </div>
    </div>
</body>
</html>
